pipeline {

    agent any

    stages {
        stage("Build"){
            steps {
                sh 'echo selam dunya'
            }
        }

        stage("Test"){
            steps {
                sh 'echo selam dunya'
            }
        }

        stage("Build Docker image"){
            steps {
                sh 'echo selam dunya'
            }
        }

        stage("Pusho docker image to dockerhub.io"){
            steps {
                sh 'echo selam dunya'
            }
        }

        stage("Deploy"){
            steps {
                sh 'echo selam dunya'
            }
        }
    }

}